_targer_ : flwr.server.strategy.FedYogi
fraction_evaluate : 1
fraction_fit : 1
min_fit_clients : ${params.num_clients_per_round_fit}
min_available_clients : 2
min_evaluate_clients : ${params.num_clients_per_round_eval}
eta : 0.01
eta_l : ${client.lr}
beta_1 : 0.9
beta_2 : 0.999
tau : 0.001

